/**
 * 라우트 전환 중 표시되는 전역 로딩 화면을 렌더링합니다.
 */

import ArchiveRouteLoading from "@/components/layout/ArchiveRouteLoading";

const Loading = () => {
  return <ArchiveRouteLoading />;
};

export default Loading;
