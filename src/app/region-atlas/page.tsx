/**
 * region-atlas 라우트의 서버 페이지를 렌더링하고 필요한 데이터와 공통 레이아웃을 연결합니다.
 */

import RegionAtlasPageShell from "@/components/regions/list/RegionAtlasPageShell";

const RegionAtlas = () => {
  return <RegionAtlasPageShell />;
};

export default RegionAtlas;
